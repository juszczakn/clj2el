(defun el2clj-replace-let ()
  (beginning-of-buffer)
  (while (search-forward-regexp "let[[:blank:]]*\\[" nil t)
    (replace-match "el2clj-let [")))

(defun el2clj-replace-lambdas ()
  (beginning-of-buffer)
  (while (search-forward "#(" nil t)
    (replace-match "(lambda (&optional % %2 %3 %4) (")
    (backward-char)
    (paredit-forward)
    (insert ")")))

(defun el2clj-replace-hash-maps ()
  (beginning-of-buffer)
  (while (search-forward "{" nil t)
    (replace-match "(el2clj-make-hash-table "))
  (beginning-of-buffer)
  (while (search-forward "}" nil t)
    (replace-match ")")))

(defun el2clj-compile-buffer ()
  (interactive)
  (let* ((cur-buf-substring (buffer-substring-no-properties 1 (point-max)))
         (cur-buf-name (buffer-file-name))
         (new-buf-name (concat cur-buf-name "2clj"))
         (new-buf (generate-new-buffer (generate-new-buffer-name "compilation-buffer"))))
    (set-buffer new-buf)
    (insert cur-buf-substring)
    (el2clj-replace-hash-maps)
    (el2clj-replace-lambdas)
    (el2clj-replace-let)
    (write-file new-buf-name)
    (kill-buffer new-buf)))
